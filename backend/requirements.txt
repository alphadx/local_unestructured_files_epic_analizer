fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.22
httpx==0.27.0
google-genai>=1.0.0
google-auth>=2.20.0
google-api-python-client>=2.100.0
requests>=2.31.0
chromadb==0.5.0
scikit-learn==1.4.2
numpy==1.26.4
# Optional: hdbscan provides advanced density-based clustering.
# It is imported dynamically and falls back gracefully if absent.
# Install separately only when HDBSCAN clustering is required.
python-magic==0.4.27
aiofiles==23.2.1
anyio==4.3.0
pytest==8.2.0
pytest-asyncio==0.23.6
# Phase 2: PostgreSQL persistence
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
# Phase 2: Celery task queue
celery[redis]==5.4.0
# Phase 2: Redis async client (pub/sub + result backend)
redis[asyncio]==5.0.4
# Phase 2: SQLite async driver for tests
aiosqlite==0.20.0
# Native fallbacks for PDF/XLSX extraction when unstructured is unavailable
pypdf==4.3.1
openpyxl==3.1.5
python-docx==1.1.0